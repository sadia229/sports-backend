# Runtime dependencies for the Vercel serverless deploy.
# Kept slim and Python 3.12-safe (Vercel builds on 3.12). Exact versions proven
# to run the app locally — only what the app imports at runtime. Heavier libs
# (supabase, sqlalchemy, redis, uvicorn) live in requirements-dev.txt and are
# added back here when their code paths go live.
fastapi==0.104.1
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0
python-dotenv==1.0.0
PyJWT==2.8.0
